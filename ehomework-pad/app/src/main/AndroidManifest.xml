<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="name.wanghwx.ehomework">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:hardwareAccelerated="false"
        android:name=".EhomeworkApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:largeHeap="true">
        <activity android:name=".view.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.MainActivity"/>
        <activity android:name=".view.activity.CourseActivity"/>
        <activity android:name=".view.activity.MistakeBookActivity"/>
        <activity android:name=".view.activity.ExerciseActivity" android:theme="@style/fullscreen"/>
        <activity android:name=".view.activity.HomeworkActivity" android:theme="@style/fullscreen"/>
    </application>
</manifest>
